#include<stdio.h>
// define a structure
struct Student{
    int rollNo;
    char name[40];
    char address[100];
};

int main(int argc, char const *argv[])
{
    int numbers[10];
    // allocation a stucture
    Student s1;
    // allocation of array of structure
    Student list[10];
    return 0;
}
